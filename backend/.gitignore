# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
pnpm-debug.log*

# Node.js runtime files and directories
node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Operating System generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE and editor directories
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Optional: Dependency directories (if you are not using a lock file or want to ignore them anyway)
# bower_components/

# Optional: Compiled files (if you use TypeScript or similar)
# *.js (if your source is TypeScript, and you compile to JS in the same folder)
# *.js.map (if you generate source maps outside of dist/)

# Optional: Test reports
# junit.xml
# test-results/

# Optional: User-specific files (like editor configurations that shouldn't be shared)
# .user
# .settings/

# Optional: Files related to specific libraries or tools
# .eslintcache
# .stylelintcache
# .prettierignore (usually this one IS versioned, but .prettierrc often is)

# dotenv vault file
.env.vault